# GO-TODO-LIST

A REST API for a todo list, written in Go — CRUD endpoints with clean routing.

## Endpoints
- `GET /todos` — list
- `POST /todos` — create
- `GET|PUT|DELETE /todos/{id}` — read / update / delete

## Run locally
```bash
go run .
```
